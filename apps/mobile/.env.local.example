# Use 127.0.0.1 only when the mobile app and API run on the same laptop/emulator.
# For Expo Go on another phone/laptop, replace this with the API laptop's LAN IP,
# for example: http://192.168.1.25:4000/v1
EXPO_PUBLIC_API_URL=http://127.0.0.1:4000/v1
EXPO_PUBLIC_EAS_PROJECT_ID=
